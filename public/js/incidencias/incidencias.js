// Obtener el campo de prioridad por su nombre y asignar el valor del código correspondiente al campo oculto del formulario
$('#prioridad').change(function()
{
	var selectedValue = $(this).val();
	$('#idPrioridad').val(selectedValue);
});

// Obtener el campo de prioridad por su nombre y asignar el valor del código correspondiente al campo oculto del formulario
$('#prioridadModal').change(function()
{
	var selectedValue = $(this).val();
	$('#idPrioridadModal').val(selectedValue);
});

// Obtener el campo de empleado por su nombre y asignar el valor del código correspondiente al campo oculto del formulario
$('#empleado').change(function()
{
	var selectedValue = $(this).val();
	$('#idEmpleado').val(selectedValue);
});

// Obtener el campo de empleado por su nombre y asignar el valor del código correspondiente al campo oculto del formulario
$('#empleadoModal').change(function()
{
	var selectedValue = $(this).val();
	$('#idEmpleadoModal').val(selectedValue);
});

// Obtener el campo de prioridad por su departamento y asignar el valor del código correspondiente al campo oculto del formulario
$('#departamento').change(function()
{
	var selectedValue = $(this).val();
	$('#idDepartamento').val(selectedValue);
});

// Obtener el campo de prioridad por su departamento y asignar el valor del código correspondiente al campo oculto del formulario
$('#departamentoModal').change(function()
{
	var selectedValue = $(this).val();
	$('#idDepartamentoModal').val(selectedValue);
});

// Obtener el campo de prioridad por su departamento y asignar el valor del código correspondiente al campo oculto del formulario
$('#facturacion').change(function()
{
	var selectedValue = $(this).val();
	$('#idFacturacion').val(selectedValue);
});

// Obtener el campo de prioridad por su departamento y asignar el valor del código correspondiente al campo oculto del formulario
$('#facturacionModal').change(function()
{
	var selectedValue = $(this).val();
	$('#idFacturacionModal').val(selectedValue);
});

// Obtener el campo de prioridad por su estado y asignar el valor del código correspondiente al campo oculto del formulario
$('#estadoModal').change(function()
{
	var selectedValue = $(this).val();
	$('#idEstadoModal').val(selectedValue);
});